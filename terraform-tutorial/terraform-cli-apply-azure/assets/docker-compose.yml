services:
  miniblue:
    # 镜像版本已锁定，避免 latest 漂移
    image: moabukar/miniblue:0.7.0
    ports:
      - "4566:4566"
      - "4567:4567"
    deploy:
      resources:
        limits:
          memory: 512M

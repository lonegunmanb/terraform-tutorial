services:
  miniblue:
    # 使用 fork 后的镜像（修复了 ARM 路由大小写敏感问题，兼容 azurerm v4），版本锁定
    image: ghcr.io/lonegunmanb/miniblue:sha-cbbbc66
    ports:
      - "4566:4566"
      - "4567:4567"
    deploy:
      resources:
        limits:
          memory: 512M

# 🎉 实验完成！

你已经掌握了 HCL 配置语法的核心知识：

## 核心概念回顾

- **块** — HCL 的基本结构单元，由块类型、标签和块体组成
- **参数** — 块体内的 `名称 = 值` 赋值语句
- **注释** — `#`（推荐）、`//`、`/* */` 三种风格
- **字符串插值** — `${}` 嵌入表达式，`$${}` 转义
- **Heredoc** — `<<-EOF` 编写多行字符串，自动去除前导空格

## 下一步

返回教程主页，继续学习 **类型** 章节。
